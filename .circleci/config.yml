
version: 2.1

parameters:
  machine:
    type: boolean
    default: false
jobs:
  first-job:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "hello"
      - run: exit 1
      
  second-job:
    docker:
      - image: cimg/base:stable
    steps:
      - run: echo "hello"

  connector:
    type: no-op
      
  say-hello:
    docker:
      - image: cimg/base:current
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"

workflows:
  say-hello-workflow:
    jobs:
      - first-job
      - connector
      - second-job:
          requires:
            - first-job:
                 - failed
            - say-hello:
                 - success
      - say-hello:
          filters: not pipeline.parameters.machine
  

      
